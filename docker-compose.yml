web:
    image: nginx
    ports:
        - "80:80"
    volumes:
        - ./html:/html
        - ./conf.d/default.conf:/etc/nginx/conf.d/default.conf
    links:
        - php
php:
    image: php:7-fpm
    ports:
        - "9000:9000"
    volumes:
        - ./html:/html